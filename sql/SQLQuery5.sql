CREATE TABLE [dbo].[Student] (
    [StudentId]          INT          NOT NULL,
    [Name]        VARCHAR (MAX)  NULL,
    [Roll] VARCHAR (8)  NULL,
    
    CONSTRAINT [PK_Products] PRIMARY KEY CLUSTERED ([StudentId] ASC)
);

